# 📰 Тематические разделы новостей

## 🎯 Задача

На небольшом новостном сайте необходимо реализовать поддержку **тематических разделов** (тегов) для статей:

- Каждая статья может относиться к нескольким разделам.
- Один из разделов должен быть отмечен как **основной**.
- В интерфейсе — в списке тегов — **основной раздел отображается первым**, остальные — **в алфавитном порядке**.
- В админке:
  - Должна быть возможность **создавать разделы**.
  - При редактировании статьи — возможность **указывать её разделы**, включая назначение одного из них основным.
  - Обязательна **валидация**: в статье **должен быть ровно один основной раздел**.

> ⚠️ Реализовала модель и логику так, чтобы шаблон заработал «из коробки».

---

## 🧱 Структура моделей

| Модель     | Описание |
|------------|----------|
| `Tag`      | Просто тег (раздел) — имеет только поле `name`. |
| `Article`  | Статья: заголовок, текст, изображение и т.п. + связь *многие-ко-многим* с `Tag`. |
| `Scope`    | Промежуточная (связующая) таблица между `Article` и `Tag`. Имеет поле `is_main` — булево, указывает, является ли данный тег основным для статьи. |

> 🔗 Связь `Article ↔ Tag` реализуется через `ManyToManyField(..., through='Scope')`.

## 🚀 Запуск проекта

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Примените миграции:
   ```bash
   python manage.py migrate
   ```

3. Загрузите тестовые данные:
   ```bash
   python manage.py loaddata articles.json
   ```

4. Запустите сервер:
   ```bash
   python manage.py runserver
   ```
